import 'package:flutter/material.dart';

class TripRecordsPage extends StatelessWidget {
  const TripRecordsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('行程记录'),
      ),
      body: const Center(
        child: Text('行程记录功能开发中...'),
      ),
    );
  }
}
